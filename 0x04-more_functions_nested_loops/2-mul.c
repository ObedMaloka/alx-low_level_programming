#include "main.h"

/**
 * mul - Multiplies two integers a & b
 * @a: First parameter
 * @b: Second parameter
 * Return: Returns the product of multiplying a & b
 */

int mul(int a, int b)
{
	int productOfMultiplication = a * b;
	return (productOfMultiplication);
}
