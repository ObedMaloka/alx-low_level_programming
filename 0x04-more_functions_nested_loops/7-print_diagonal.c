#include "main.h"

/**
 * print_diagonal - Draw a diagonal line on the terminal
 * @n: Parameter for drawing diagonal line
 * Return: Returns void after execution
 */

void print_diagonal(int n)
{
	if (n > 0)
	{
		_putchar(
	}
}
