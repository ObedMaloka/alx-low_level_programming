#include "main.h"

/**
 * swap_int - Swaps the values of two integers
 * @a: First parameter
 * @b: Second parameter
 * Return: 0 always
 */

void swap_int(int *a, int *b)
{
	m = *a;
	*a = *b;
	*b = m;
}
