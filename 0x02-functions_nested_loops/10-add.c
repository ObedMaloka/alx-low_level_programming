#include "main.h"

/**
 * add - Adds two integers and returns the result
 * @a: First parameter
 * @b: Second parameter
 * Return: Returns result of add function
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}

